name: GitHub Pages Disabled

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain why Pages deploy is disabled
        run: |
          echo "GitHub Pages deployment is disabled."
          echo "This app now uses dynamic Next.js routes for /courses."
          echo "Dynamic routes require a server-capable host and do not produce ./out for Pages."
